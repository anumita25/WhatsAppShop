import { FlatList, Pressable, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Brand, Card, Heading, Pill, Screen, palette } from '@/components/shop-ui';
import { Order, toneForStatus, useShop } from '@/context/shop-store';

export default function Orders() { const { orders, advanceStatus } = useShop(); return <Screen><SafeAreaView style={styles.safe} edges={['top']}><FlatList data={orders} keyExtractor={(item) => item.id} showsVerticalScrollIndicator={false} contentContainerStyle={styles.content} ListHeaderComponent={<><Brand /><Heading eyebrow="Order management" title="Orders" /><Text style={styles.date}>ALL ORDERS · {orders.length}</Text></>} renderItem={({ item }) => <OrderCard order={item} onAdvance={() => advanceStatus(item.id)} />} /></SafeAreaView></Screen>; }
function OrderCard({ order, onAdvance }: { order: Order; onAdvance: () => void }) { const final = order.status === 'Delivered'; return <Card style={styles.orderCard}><View style={styles.orderTop}><View><Text style={styles.orderId}>{order.id}</Text><Text style={styles.name}>{order.name}</Text></View><Pill label={order.status} tone={toneForStatus(order.status)} /></View><Text style={styles.item}>{order.item}</Text><View style={styles.orderBottom}><Text style={styles.time}>Received {order.received}</Text>{final ? <Text style={styles.complete}>Complete</Text> : <Pressable onPress={onAdvance} hitSlop={6}><Text style={styles.advance}>Mark {order.status === 'New' ? 'confirmed' : order.status === 'Confirmed' ? 'packed' : 'delivered'}</Text></Pressable>}</View></Card>; }
const styles = StyleSheet.create({ safe: { flex: 1 }, content: { padding: 20, gap: 10, paddingBottom: 132, maxWidth: 720, width: '100%', alignSelf: 'center' }, date: { color: palette.muted, fontSize: 11, fontWeight: '800', letterSpacing: .9, marginTop: 20, marginBottom: 2 }, orderCard: { gap: 12 }, orderTop: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-start' }, orderId: { color: palette.muted, fontSize: 11, fontWeight: '700', marginBottom: 3 }, name: { color: palette.ink, fontSize: 16, fontWeight: '800' }, item: { color: palette.muted, fontSize: 13 }, orderBottom: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', borderTopWidth: 1, borderTopColor: '#EEF3F0', paddingTop: 11 }, time: { color: palette.muted, fontSize: 12 }, advance: { color: palette.green, fontSize: 12, fontWeight: '800' }, complete: { color: palette.muted, fontSize: 12, fontWeight: '700' } });
